import {StatusExpirationEnum} from '../../shared/common/api/enum/status.expiration.enum';

export class ExpirationDetail {

    colorDefined: string;
    expirationDescriptionDate: string;
    statusExpiration: string;
    statusExpirationFromUser: StatusExpirationEnum;
}